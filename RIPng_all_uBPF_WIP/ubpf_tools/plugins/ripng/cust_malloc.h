#ifndef CUST_MALLOC_H
#define CUST_MALLOC_H

#include <stdio.h>
#include <stdlib.h>
#include "../../include/public_bpf.h"

/*void init_mal(struct mal** init, struct mal** last);

void* cust_malloc(size_t s, struct mal* init, struct mal** last);

void* cust_calloc(size_t s, struct mal* init, struct mal** last);

void free_mal(struct mal* cur);

void close_mal(struct mal** init, struct mal** last);*/

#endif // CUST_MALLOC_H