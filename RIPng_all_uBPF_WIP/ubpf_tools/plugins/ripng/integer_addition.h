#ifndef FRR_INTEGER_ADDITION_H
#define FRR_INTEGER_ADDITION_H

#include <stdlib.h>
#include "class_algebra.h"
#include "cust_malloc.h"

/**
 * create an algebra that adds metrics along paths and selects the smallest
 * @param n: the minimum value of a signature
 * @param m: the maximum value of a signature
 * @return an algebra
 */
//algebra* ADD(int n, int m, struct mal* init, struct mal** last);

#endif // FRR_INTEGER_ADDITION_H
